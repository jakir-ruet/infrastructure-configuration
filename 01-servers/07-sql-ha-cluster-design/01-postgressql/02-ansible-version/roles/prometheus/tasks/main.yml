- name: Install Prometheus
  ansible.builtin.apt:
    name: prometheus
    state: present

- name: Start Prometheus
  ansible.builtin.systemd:
    name: prometheus
    state: started
    enabled: true
