# Allow replication connections from replica servers
# TYPE  DATABASE        USER            ADDRESS                 METHOD
host    replication     replicator      192.168.1.118/32        scram-sha-256
host    replication     replicator      192.168.1.119/32        scram-sha-256

# Allow replication from entire subnet (less strict)
host    replication     replicator      192.168.1.0/24          scram-sha-256
