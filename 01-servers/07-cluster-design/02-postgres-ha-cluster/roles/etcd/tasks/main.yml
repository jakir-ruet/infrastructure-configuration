---
- name: Download etcd (ARM)
  ansible.builtin.get_url:
    url: "https://github.com/etcd-io/etcd/releases/download/v{{ etcd_version }}/etcd-v{{ etcd_version }}-linux-arm64.tar.gz"
    dest: /tmp/etcd.tar.gz
    mode: '0755'

- name: Extract etcd
  ansible.builtin.unarchive:
    src: /tmp/etcd.tar.gz
    dest: /usr/local/bin/
    remote_src: yes
    creates: /usr/local/bin/etcd

- name: Create data directory
  ansible.builtin.file:
    path: "{{ etcd_data_dir }}"
    state: directory
    owner: "{{ ansible_user_id }}"
    group: "{{ ansible_user_id }}"
    mode: "0755"

- name: Create systemd service
  ansible.builtin.copy:
    dest: /etc/systemd/system/etcd.service
    owner: root
    group: root
    mode: "0644"
    content: |
      [Unit]
      Description=etcd
      After=network.target

      [Service]
      Type=notify
      ExecStart=/usr/local/bin/etcd \
        --name {{ inventory_hostname }} \
        --data-dir {{ etcd_data_dir }} \
        --initial-advertise-peer-urls http://{{ ansible_host }}:2380 \
        --listen-peer-urls http://{{ ansible_host }}:2380 \
        --listen-client-urls http://{{ ansible_host }}:2379,http://127.0.0.1:2379 \
        --advertise-client-urls http://{{ ansible_host }}:2379 \
        --initial-cluster {% for n in etcd_nodes %}{{ n.name }}=http://{{ n.ip }}:2380{% if not loop.last %},{% endif %}{% endfor %} \
        --initial-cluster-token {{ etcd_cluster_token }} \
        --initial-cluster-state new

      [Install]
      WantedBy=multi-user.target

- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: yes

- name: Enable and start etcd
  ansible.builtin.systemd:
    name: etcd
    enabled: yes
    state: started
